# QuantSpec environment template
# Copy to .env for local use. Do not commit real secrets.

# Optional. Required only for future live LLM mode.
ANTHROPIC_API_KEY=

# Default mode for future CLI runs: fixture or live.
QUANTSPEC_LLM_MODE=fixture
